import styled from "styled-components/macro";
import tw from "tailwind.macro";

const Label = styled.label`
  ${tw`block mb-1 font-medium flex flex-col lg:flex-row lg:justify-between`}
`;

export default Label;
