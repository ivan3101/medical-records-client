import React from "react";
import styled from "styled-components/macro";
import tw from "tailwind.macro";

const Card = styled.div`
  ${tw`rounded h-auto bg-white w-auto max-w-sm h-auto shadow-md`}
`;

export default Card;
