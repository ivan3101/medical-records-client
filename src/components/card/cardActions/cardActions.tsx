import styled from "styled-components/macro";
import tw from "tailwind.macro";

const CardActions = styled.div`
  ${tw`flex flex-row max-w-full items-center px-6 py-3`};
`;

export default CardActions;
