import styled from "styled-components";

const Icon = styled.div`
    display: inline-flex;
    align-self: center;
    margin-right: 5px;

    svg, img {
      height: 2em;
      width: 2em;
      fill: currentColor;
      top: .125em;
      position: relative;
    }
`;

export default Icon;
